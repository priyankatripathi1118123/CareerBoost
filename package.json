{
  "name": "prepai-platform",
  "version": "1.0.0",
  "description": "Production-Ready Placement Preparation & Job Portal",
  "scripts": {
    "install:all": "npm install --prefix frontend && pip install -r requirements.txt",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "python backend/app.py",
    "start": "python run.py"
  }
}
